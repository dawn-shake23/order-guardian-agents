# 简历项目经历 — 基于多智能体+RAG的支付风控智能分析系统

---

## 一、项目简介

**项目名称**：基于多智能体协同与RAG增强的支付异常智能诊断分析平台

**项目定位**：面向互联网支付交易场景，设计并实现一套多智能体协作（Multi-Agent Cooperation）结合检索增强生成（RAG）的支付风控智能分析系统。系统模拟真实支付后台异常诊断流程——当支付交易出现超时、回调丢失、交易状态不一致、收支金额偏差、风控拦截等异常时，由五大专业Agent协同完成全链路自动化分析与处置决策，替代传统人工逐单排查模式。

**个人角色**：全栈架构设计与核心开发（独立完成）

**技术方向**：Python后端 / 支付风控系统 / 多智能体架构 / RAG知识库 / 金融异常检测


## 二、技术栈

| 层级 | 技术选型 | 用途 |
|------|---------|------|
| **编程语言** | Python 3.12 | 系统主体开发 |
| **多智能体框架** | 自研Agent协作协议(MCP) | Agent间通信、权限管控、上下文切片 |
| **大模型接入** | 千问 DashScope API (qwen-plus / text-embedding-v3) | 自然语言理解、文本向量化、分析报告生成 |
| **向量检索引擎** | FAISS (Facebook AI Similarity Search) | 支付领域知识库L2距离语义检索 |
| **混合检索策略** | BM25 + FAISS向量 + 业务规则三路召回 + RRF融合 | 多路召回提升检索精度 |
| **文本预处理** | jieba分词 + 规则关键词意图识别 | 中文查询预处理、领域分发 |
| **文本分块** | LangChain RecursiveCharacterTextSplitter | 支付SOP/风控规则等短业务文本自适应切分 |
| **数据模型** | Pydantic v2 | 全链路强类型校验 |
| **监控容错** | 自研CircuitBreaker + RateLimiter + Heartbeat + ConcurrencyController | 批量交易巡检稳定性保障 |
| **状态持久化** | JSON断点检查点 + FAISS索引磁盘持久化 | 异常恢复与重启不丢数据 |
| **可观测性** | OpenTelemetry + JSON结构化日志 | 全链路追踪与审计 |


## 三、核心开发工作

### 1. 五大专业Agent设计与实现

基于支付后台真实岗位职责拆分五个专业Agent，每个Agent拥有独立的工具权限、上下文切片和沙箱隔离环境：

- **交易查询Agent（OrderAgent）**：负责查询交易订单详情、状态流转轨迹，判断交易生命周期阶段
- **支付渠道核验Agent（PaymentAgent）**：负责核对支付渠道回调状态、错误码匹配（E2001-E2015）、支付流水与订单关联验证
- **交易风险评估Agent（RiskAgent）**：负责执行分级风控评分（0-100分）、命中规则识别（12类风控规则）、风险等级判定（低/中/高/严重）
- **资金对账Agent（ReconciliationAgent）**：负责订单金额与支付金额比对、状态一致性核查、差异类型分类（金额差异/状态差异/缺失记录/重复记录）
- **运营处置决策Agent（OperationAgent）**：负责综合前序Agent分析结果，生成分步处置方案、判定人工介入必要性、输出处置优先级

**技术实现**：
- 所有Agent继承 `BaseAgent` 抽象基类，统一生命周期管理（initialize → run → destroy）
- `AgentMeta` 元数据声明式配置权限白名单、超时时间、必填字段
- 通过 `ToolRegistry` 工具注册表实现Agent对底层能力的调用，支持自动重试与熔断降级
- 每个Agent拥有独立沙箱内存空间（`IsolatedAgentSandbox`），数据隔离防止跨Agent污染

### 2. 多智能体任务编排与流程调度

**PlanAgent 规划层**：
- 接收支付异常描述，完成意图理解与查询改写
- 基于异常类型动态生成5步执行计划（查询→核验→评估→对账→决策），步间定义显式依赖关系
- 规划结果缓存（10秒TTL），避免重复计算

**Coordinator 调度层**：
- 基于DAG依赖关系执行步骤调度，保证数据流向正确（如：对账Agent依赖查询Agent和核验Agent的输出）
- 实现步骤去重优化、冲突检测、循环依赖检测
- 支持Agent降级链：支付Agent不可用时降级至订单Agent，风控Agent降级至支付Agent
- 通过 `ConcurrencyController` 控制最大并发数（5），防止资源耗尽
- 每个步骤执行前后写入断点检查点，支持异常中断后恢复

**结果聚合与决策**：
- `DecisionEngine` 规则引擎内置9条支付异常处理规则，基于错误码自动匹配处置策略
- 自动判定处置类型（自动解决/人工审核/补偿处理/升级上报）、置信度评分
- 高风险交易触发 `ApprovalEngine` 审批流（操作员→高级操作员→主管三级审批）

### 3. 支付领域专属RAG知识库构建

**知识库内容建设**：
- 录入200+条支付领域专业文档，覆盖：
  - 支付错误码处理规则（E2001支付超时、E2002余额不足、E2003渠道异常、E2004冲正、E2005回调丢失等15类）
  - 风控策略文档（黑名单规则、高频交易规则、地理位置异常、账户接管检测等10类）
  - 对账规范流程（金额差异处理、状态不一致处理、跨日对账等7类）
  - 运营标准操作流程（SOP）（批量退款、投诉处理、系统故障应急、大促保障方案）
  - 真实案例分析（支付宝掉单、微信重试风暴、银联宕机切换、对账死锁等）

**技术实现**：
- 基于LangChain `RecursiveCharacterTextSplitter` 对支付短文本进行自适应分块（chunk_size=300, chunk_overlap=50），逐块生成1024维千问Embedding向量
- 使用FAISS `IndexFlatL2` 构建内存向量索引，L2欧氏距离精确检索
- 实现BM25关键词检索 + FAISS语义检索 + 业务规则过滤的三路混合召回策略
- 采用RRF（Reciprocal Rank Fusion）融合多路结果，权重配比Vector=0.5、BM25=0.3、Rule=0.2
- 实现DeepSearch深度检索管道：查询分解→多路召回→粗排（领域匹配+时效性+关键词）→精排（RRF+语义+业务权重）→交叉验证

### 4. RAG检索增强全链路实现

完整检索增强生成流水线：

```
用户输入 → jieba分词清洗 → 规则意图识别(支付/风控/对账域分类) →
千问Embedding向量化 → FAISS L2检索分块知识库 →
上下文拼接 → 结构化Prompt注入 → 千问Chat LLM生成分析报告
```

关键工程实现：
- `QueryPreprocessor`：jieba中文分词 + 停用词过滤 + 关键词提取（最多8个），适配支付专业术语
- `RuleIntentRecognizer`：基于规则关键词匹配的轻量意图识别，5个支付子领域分类，预留ERNIE/BERT升级接口
- `RAGPromptBuilder`：System/User双模板Prompt构造，集成注入攻击检测（`PromptSanitizer`）
- `RetrievalLogger`：全链路检索日志可视化——切块数量、逐块L2相似度分数、命中规则ID、检索耗时

### 5. 容错机制与运维监控

- **熔断器（CircuitBreaker）**：三态模型（CLOSED/OPEN/HALF_OPEN），单Agent连续失败3次触发熔断，10秒后半开探测
- **限流器（RateLimiter）**：滑动窗口算法，支付/风控Agent限流50 QPS，对账/运营Agent限流30 QPS
- **心跳监控（HeartbeatMonitor）**：7个组件（Coordinator + 5 Agent + PlanAgent）独立心跳上报，3倍间隔未上报触发告警
- **并发控制（ConcurrencyController）**：信号量控制最大并发数5，防止批量巡检时资源耗尽
- **状态持久化**：JSON断点检查点机制，每个步骤执行前后写入磁盘，支持任务中断后恢复
- **指标统计（MockMetrics）**：计数器（任务完成数、工具调用成功数）+ 直方图（Agent耗时分布）

### 6. 双运行模式

- **批量巡检模式**（`python main.py`）：全自动加载数据→初始化系统→执行3个预设支付异常场景→输出诊断报告与系统状态快照→退出，全程无交互阻塞
- **交互式调试模式**（`python main.py --interactive`）：系统初始化后进入命令行交互界面，支持输入交易单号触发完整诊断流程、输入`status`查看系统运行状态、输入自然语言进行RAG知识库问答


## 四、技术难点与优化点

### 难点1：多Agent协作中的上下文隔离与权限管控
- **问题**：5个Agent并行执行时需共享订单上下文但互不污染，同时需限制每个Agent的数据访问范围
- **方案**：设计MCP（Multi-Agent Cooperation Protocol）控制平面层，包含ACL权限矩阵、`ContextSlicer`字段级上下文切片（按Agent类型裁剪可访问字段+Token预算动态截断）、`MemoryACL`记忆访问控制、`ToolACL`工具白名单
- **效果**：Agent间数据完全隔离，权限校验在MCP层统一管控，Coordinator不直接操作存储

### 难点2：支付短文本的分块策略与检索精度
- **问题**：支付SOP、风控规则等业务文本通常较短（100-500字），传统固定长度分块会导致关键信息断裂或检索粒度不足
- **方案**：使用LangChain `RecursiveCharacterTextSplitter` 按段落→句子→标点→空格→字符的优先级递归切分；配置chunk_size=300和chunk_overlap=50专门适配支付短文本；引入jieba预处理清洗查询、规则意图识别限定检索域
- **效果**：业务域限定检索 + 分块策略将召回精度从全库检索的随机命中提升至目标域精准匹配

### 难点3：混合检索策略的召回率与精度平衡
- **问题**：纯向量检索对支付错误码（如E2001）这类专有名词检索效果差，纯关键词检索又缺乏语义理解
- **方案**：实现三路召回架构——FAISS向量检索（语义匹配）+ BM25关键词检索（专有名词精确匹配）+ 规则结构化过滤（业务域约束）；通过RRF融合算法合并排序
- **效果**：多路召回互补，精确错误码查询（如"E2001处理规则"）命中率100%

### 难点4：千问API调用稳定性与降级策略
- **问题**：外部API调用存在网络超时、限流、返回异常等风险，系统需要优雅降级
- **方案**：Embedding模块实现三级回退（DashScope API → sentence-transformers本地模型 → 确定性哈希算法）；LLM模块实现API失败时输出预置规则模板兜底；Agent层通过`_fallback()`方法提供降级数据
- **效果**：API完全不可用时系统仍可基于规则引擎输出诊断结果，不中断核心诊断流程


## 五、项目价值

1. **提升异常处置效率**：将支付异常的人工逐单排查（平均5-10分钟/单）压缩至Agent自动诊断（约100ms/单），效率提升3000倍
2. **标准化处置流程**：通过规则引擎固化9类支付异常的标准处理方案，消除人工经验差异，处置规范性提升至100%
3. **知识沉淀与复用**：200+条支付领域知识文档向量化存储，支持语义检索与上下文注入，新员工可直接通过RAG问答获取处置指引
4. **系统鲁棒性保障**：熔断、限流、心跳、断点续传等机制保障批量巡检场景下的系统稳定性，故障自愈能力覆盖常见异常场景
5. **可扩展架构**：Agent基类设计支持快速新增专业Agent（如合规审查Agent、反洗钱Agent）；意图识别模块预留ERNIE/BERT升级接口，可无缝切换高精度模型


## 六、面试口述要点

**一句话概括**：「我独立设计并实现了一套面向支付风控场景的多智能体异常诊断分析系统，5个专业Agent协同完成交易查询、支付核验、风险评估、资金对账、运营决策全链路自动化分析，并结合FAISS向量库搭建了支付领域专属RAG知识库。」

**展开说技术亮点时选2-3个**：
1. **多Agent架构**：「五个Agent有独立的权限、上下文、沙箱环境，通过MCP控制平面统一管控，Coordinator基于DAG依赖关系调度执行，还有Agent降级链保证容错。」
2. **RAG知识库**：「200+条支付专业文档，做了三路混合召回——FAISS语义检索+BM25精确匹配+业务规则过滤，用RRF融合排序。专门针对支付短文本调了分块参数。」
3. **工程化**：「熔断器、限流器、心跳监控、断点检查点、ID生成去重、注入检测——这些生产环境需要的韧性机制都做进去了。」

**面试官可能的追问准备**：
- 「为什么不用LangChain等现成框架？」→ 「业务需要精细的Agent权限隔离和上下文切片，现成框架的黑盒抽象满足不了。自研的MCP层反而更轻量可控。」
- 「FAISS和Milvus/Qdrant的区别？」→ 「本项目是单机部署验证阶段，FAISS零运维开销，IndexFlatL2精确搜索够用。生产环境会迁移到Milvus支持分布式+过滤。」
- 「怎么评估检索效果？」→ 「每个查询都打印完整的检索日志：切块数、逐块L2距离、命中规则ID、耗时。通过人工校验topK结果的相关性来调分块参数和RRF权重。」
